fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.24
pydantic>=2
websockets>=12
diart==0.9.2
pyannote.audio==3.3.2
huggingface_hub<0.26

# Install torch and torchaudio before this file, using matching versions.
# Current clean validation environment:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.4.1 torchaudio==2.4.1
# CUDA wheel install was attempted first but did not complete in this container.
